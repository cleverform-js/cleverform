## Playground for CleverForm  core compiled/bundled library.

- test the library in different settings, combinations of rules.
- test the library in different CSS libraries.
- test the library in XHR or POST request.